# TODO / メモ

最終更新: 2026-08-21

## 現状

設計メモ（バックログ6項目）は一通り実装済み:

- [x] インデックス設定ファイル (`config.json`, ルート/走査深度/除外パス)
- [x] 拡張子ホワイトリスト (`.exe` / `.lnk`、フォルダは常に対象)
- [x] migemo 組み込み (cmigemo v1.7.0 x64 を FFI で呼び出し)
- [x] 既定除外リスト + 走査深度制限 (node_modules / obj / bin / .git 等)
- [x] アイコン取得 (`SHGetFileInfo`)
- [x] 検索ロジック本体 (migemo 正規表現 + 実ファイルインデックスのマッチング)

追加で起動キーを `config.json` の `Hotkey` で設定可能にした（既定 CapsLock、修飾キー付きの任意のキーにも変更可）。
Alt+Space の常時併用（旧 `HotkeyManager.cs`）は不要とのことで削除し、`GlobalHotkey.cs` 1本に統合済み。
ショートカットコマンド（`/s` = インデックス再構築、`//<語>` = ブラウザ検索）を `CommandRouter.cs` として実装、Enter 押下時にのみ判定する。
アプリ独自アイコン（`Assets/AppIcon.ico`）を作成し、exe アイコン・タスクトレイアイコン両方に適用済み（既定の `SystemIcons.Application` から変更）。
検索結果のランキング（使用回数→最終使用日時、未使用は既存の走査順のまま）を `UsageStore.cs` として実装。`%AppData%\OriginalLauncher\usage.json` に永続化。
検索結果の重複統合（表示名+種別が完全一致するものを1件にまとめる）を `MainWindow.QueryBox_TextChanged` に実装。別ルート（デスクトップ/共通スタートメニュー/ユーザースタートメニュー）に同名のショートカット・フォルダが重複して並ぶ問題（RayCast でも同様に起きる、とのユーザー指摘）に対応。インデックス自体は変更せず表示時のみグルーピングする。
タイプ入力時の引っかかり対策: 1 打鍵ごとにフル検索（正規表現マッチ + 並べ替え）とアイコン取得（`SHGetFileInfo`）を同期実行していたのが原因だったため、(1) `MainWindow` に検索デバウンス（60ms、入力が止まってからまとめて検索）を追加、(2) `ResultItem`/`IconProvider` のアイコン取得をバックグラウンドスレッドで非同期化（`IconProvider` のキャッシュも `ConcurrentDictionary` 化）。
→ この非同期化の副作用で「何度か使っているとアイコンが出なくなる」不具合が発生（ユーザー報告、2026-08-21）。原因は `SHGetFileInfo` がシェル API で複数スレッドから同時に呼ぶと不安定になり得ること、かつ失敗（null）をそのままキャッシュしていたため一度失敗したパスは永久にアイコンなしになっていたこと。`IconProvider.cs` に `SemaphoreSlim` を追加して実際の取得処理を 1 件ずつ直列化し、失敗時はキャッシュしない（リトライ可能にする）よう修正。25 種類のクエリを連続実行するストレステストで再発しないことを確認済み。
設定画面 (`SettingsWindow.xaml(.cs)`) を実装。`/o` またはトレイメニュー「設定...」から開く。起動キー・インデックス対象ルート（DataGrid で追加/編集/削除、フォルダ選択ダイアログ対応）・最大表示件数 (`MaxResults`)・検索ショートカット (`SearchShortcuts`、fenrir 時代の `/m`=GoogleMap `/a`=Amazon のような運用を再現) を編集可能。保存すると config.json に書き込み、アプリ再起動なしで反映される（`MainWindow.ReloadFromConfig()` でインデックス再構築+コマンド再登録、`App` 側でホットキーを Dispose→再 Install）。「/m Tokyo Tower」等、保存直後に実際に動作することを確認済み。

### 解決済み: 検索結果の上下矢印キー選択が効かない (2026-08-22)

ユーザー指摘により判明。実装自体は最初からあった（`Key.Down`/`Key.Up` → `MoveSelection`）が、実は一度も実キー入力で検証できておらず（過去のテストは `PostMessage` や UI Automation の `SelectionItemPattern.Select()` 越しで、いずれもキーボード入力の実処理を経由していなかった）、実際には機能していなかった。

原因: WPF の `TextBox` は単一行 (`AcceptsReturn=False`) でも、内部の `TextEditor` が上下矢印キーの既定キーバインド（行移動コマンド）を持っており、フォーカスのある `TextBox` を通過する際に自ら `e.Handled = true` にしてしまう。そのため `Window` の `KeyDown`（バブルフェーズ、`TextBox` の既定処理より後）ではイベントが届かなかった。`PreviewKeyDown`（トンネルフェーズ、`TextBox` の既定処理より先）で拾うよう `MainWindow.xaml` / `.xaml.cs` を修正し、実際のキー入力（`SendKeys` 経由）で ↓↓↑ が選択を正しく移動する（0→1→2→1）ことを確認。通常の文字入力・Escape・Enter が引き続き正常に動作することも回帰確認済み。

### ショートカットコマンドの挙動変更 (2026-08-22)

ユーザー要望により、コマンドの実行タイミングを Enter 押下時から「入力中の即時反応」に変更した。

- **引数なしコマンド (`/s` 再構築、`/o` 設定画面)** — 文字列がちょうど一致した瞬間に即実行される（Enter 不要）。`CommandRouter.cs` を「完全一致→即実行」のみのシンプルな実装に作り直し、`RegisterPrefixed`/`QueryCommandHandler` は廃止。
- **検索ショートカット (`//` `/m` `/a` 等、検索語が必要なもの)** — プレフィックスちょうどを入力すると「待機状態」に入る: 入力欄が自動でクリアされ、右側に `SearchShortcutConfig.Description`（新設フィールド、設定画面の DataGrid でも編集可）を表示する。続けて検索語を入力して `Enter` で実行、`Esc` は待機状態だけ解除してポップアップは閉じない（誤入力からの一段階の後戻り）。`MainWindow` に `_armedShortcut` 状態を追加し、`SearchShortcuts` は照合のたびに `_config.SearchShortcuts` を直接見るため設定変更後の再登録が不要になった（`ReloadFromConfig()` はインデックス再構築のみで済むよう簡略化）。
  実機同等の検証（UI Automation + 実際のブラウザ起動確認）で、`/s` の即時実行、`//` での待機状態突入とヒント表示、検索語入力→Enter での正しい URL 起動、Esc 一段階戻りをすべて確認済み。

### 解決済み: 再表示時に直前の入力内容が一瞬フラッシュする (2026-08-22)

ユーザー指摘: 文字を入力したまま起動キーで閉じ、再度起動キーで開くと、クリアされる前の古い入力内容が一瞬だけ見える。

原因: `HidePopup()` が `Hide()` するだけで入力欄をクリアしておらず、クリア自体は次回 `ShowPopup()` 側で行っていた。`Hide()` は直前に描画されたフレームをそのまま保持するため、次に `Show()` した瞬間、まだ新しい（クリア後の）フレームが描画される前に、Windows がキャッシュされた古いフレームを一旦表示してしまう。

修正: `HidePopup()` 内で「隠す前に」入力欄をクリアし、`Dispatcher.Invoke(() => {}, DispatcherPriority.Render)` で実際に描画が完了するのを待ってから `Hide()` するよう変更。これにより、隠れた時点で既にクリア後の状態がキャッシュされるため、次回表示時に古い内容がフラッシュしない想定。
UI Automation でクリア自体（論理的な状態）が再表示直後に空であることは確認済みだが、単一フレーム単位の視覚的なフラッシュそのものはスクリーンショット越しの自動検証では捉えられないため、実機での見た目の確認待ち。

### 結果選択時のフォルダオープン機能 + 右クリックメニュー (2026-08-22)

要望: 検索結果を選んでいる状態で `Ctrl+Enter`、または結果を右クリックして「対象を含むフォルダを開く」を実行できるようにしたい。

- `Ctrl+Enter` は `Window_PreviewKeyDown` の `Key.Enter` 分岐で `Keyboard.Modifiers` を見て `OpenContainingFolderForSelected()` に振り分け。
- 右クリックは当初 `ListBox.ItemContainerStyle` に項目ごとの `ContextMenu` を `EventSetter`(`PreviewMouseRightButtonDown` で選択状態にする)と組み合わせて実装したが、この透過(`AllowsTransparency`)+最前面(`Topmost`)のボーダレスウィンドウ上ではマウスのキャプチャ/ルーティングと干渉するのか、右クリックしてもメニューが一切開かない不具合が発生。`ListBox` 本体に `ContextMenu` を1つだけ持たせ、`ContextMenuOpening` イベント + ヒットテストで対象項目を選択状態にしてから開く、より標準的な WPF パターンに書き換えて解消（自動化した合成マウスクリックでは最終確認しきれておらず、実機での動作確認は依頼中）。
- 「対象を含むフォルダを開いてハイライトする」は `explorer.exe /select,"パス"` で実装。`ProcessStartInfo.ArgumentList` 経由だと `/select,` ごと丸ごとクォートされてしまい、`explorer.exe` 独自の（`CommandLineToArgvW` 非準拠の）引数解釈と噛み合わず既定の「ドキュメント」フォルダにフォールバックしていた。生の `Arguments` 文字列（`/select,` の直後だけをクォート）に変更して解消、Chrome のショートカットで実際に正しいフォルダが開き対象がハイライトされることを自動検証済み。
- ついでに検索結果の重複統合キー（[MainWindow.xaml.cs:258](src/OriginalLauncher/MainWindow.xaml.cs#L258)）に **ドライブ文字** を追加。従来は表示名+種別のみで統合していたため、`C:\...\foo.lnk` と `D:\...\foo.lnk` のような別ドライブの同名別ファイルまで誤って1件にまとめてしまっていた。`.lnk`/`.exe` はもともと `Kind` が異なるため統合されない。
- また `_foregroundWatcher`（前面ウィンドウをポーリングして他アプリに切り替わったらポップアップを閉じる仕組み）が、右クリックメニューのような「自プロセス内の別ウィンドウ」に前面が移っただけでも誤って閉じてしまうバグを発見。前面ウィンドウの所有プロセスIDで比較する方式に修正（ウィンドウハンドルの完全一致ではなく、自プロセス内かどうかで判定）。

### インデックスのキャッシュ化による起動高速化 (2026-08-22)

ユーザー指摘: ランチャーの起動が遅い。毎回インデックスを再構築しているのでは？ → その通りで、`MainWindow` のコンストラクタで毎回同期的に `FileIndexer.BuildIndex()`（全ドライブ走査）を実行していたのが原因だった。

修正: [IndexStore.cs](src/OriginalLauncher/IndexStore.cs) を新設し、走査結果を `%AppData%\OriginalLauncher\index.json` に永続化。次回起動時はまずこのキャッシュを読み込むだけで済ませ（走査しない）、キャッシュが無い場合（初回起動、または index.json を削除した場合）のみバックグラウンドスレッドで実際の走査を行い、その間はトレイアイコンのバルーンチップで「初回起動のためインデックスを作成しています…」と通知する。`_index` は起動時点では空リストで即座に返し、読み込み/走査は非同期（`LoadIndexAsync`）で行うため、起動自体（ホットキー登録・トレイアイコン表示）はブロックしない。`/s`（手動再構築）・設定画面保存後の `ReloadFromConfig()` は引き続き同期的に再走査するが、その都度 `index.json` も更新するようにした。
実際に index.json が無い状態と有る状態の両方で起動し、無い場合はバックグラウンドで作成されること、有る場合は即座に検索できることを自動検証済み。

## 未着手・気になっている点

- **設定画面のホットキー入力はテキストボックス手打ち** — 実際にキーを押して検知するキャプチャ UI ではなく、`"CapsLock"` 等のキー名を文字列で入力する方式。タイポに気づきにくい。
- **拡張子ホワイトリスト (`Extensions`) と既定除外名 (`ExcludeNames`) は設定画面から編集不可** — 現状 config.json の直接編集のみ。優先度は低いが要望が出たら追加する。
- **frecency ランキングの調整余地** — 現状は「使用回数→最終使用日時」の単純な二段ソート。本格的な frecency（経過時間による減衰など）にしたくなったら `UsageStore.cs` の `GetCount`/`GetLastUsedUtc` の使い方を見直す。
- **アイコンキャッシュが常駐中増え続ける** — `IconProvider` の静的 `Dictionary` はプロセス終了まで解放されない。長時間常駐 + 大量の異なるファイルを検索し続けるケースでの挙動は未検証。
- **単体テスト・自動テストが皆無** — 現状はすべて手動 (UI Automation スクリプトでの一時的な検証) で確認している。
- **配布・インストーラー未検討** — `dotnet publish` でのパッケージング、自動起動登録（スタートアップ）は未着手。
- **CapsLock 完全乗っ取りにより、常駐中は本来の CapsLock（大文字固定）が一切使えない** — 意図した仕様だが、将来 Shift+CapsLock 併用案が欲しくなったら [[GlobalHotkey.cs]] を拡張する。

### 解決済み: CapsLock 起動時の不具合三点セット (2026-08-21)

実機での再現により、下記3つの不具合が同時に見つかり、根本原因を特定して修正した。

1. **CapsLock を押すと ON になり大文字入力になる**
2. **一度 ON になると以降 CapsLock を押しても OFF に戻らず固着する**
3. **【最も深刻】入力欄にフォーカスがある状態でランチャーを起動すると、入力が起動前のウィンドウの入力欄に流れ続け、ランチャー側に文字が入力されない**

根本原因は主に2つ:

- **フックコールバック内で `Show()`/`SetForegroundWindow` 等の重い処理を同期実行していた** — WH_KEYBOARD_LL のコールバックは高速に返さないと Windows のメッセージポンプがネストし、遅れて届く KEYUP が再入的に配送されたり、swallow（握りつぶし）の戻り値が正しく効かなくなったりする。これが上記1〜3すべての遠因だった。
  → `App.xaml.cs` で `Triggered` を `Dispatcher.BeginInvoke` 経由に変更し、フック呼び出しの外側で実行するよう修正。
- **離上(KEYUP)まで握りつぶしていた** — OS 側が「押されたまま」と誤認し、トグルの固着（不具合2）につながっていた。
  → `GlobalHotkey.cs` で離上は常に素通しするよう修正。

その他の修正:

- `Window.Activate()` だけではフォアグラウンドロックにより黙って失敗することがあったため、`WindowActivator.cs`（Alt キーの疑似押下 + `AttachThreadInput` の併用）で確実に前面化・フォーカスするようにした。
- `Window.Activated`/`Deactivated` イベントは強制フォーカスと組み合わせると不安定だったため使うのをやめ、`GetForegroundWindow` を 200ms 間隔でポーリングして実際に前面でなくなったら閉じる方式（`MainWindow` の `_foregroundWatcher`）に変更。表示直後 400ms は判定を猶予する。
- 修飾キーの状態取得を `GetKeyState`（スレッドのメッセージキュー依存で古い状態が残ることがある）から `GetAsyncKeyState`（リアルタイム）に変更。

上記修正後、実機再現と同条件（他アプリの入力欄にフォーカスがある状態から CapsLock でランチャーを起動）で再検証し、**CapsLock のトグル状態が一切変化しない・フォーカスが正しく QueryBox に移る・クリックで正しく閉じる**ことを確認済み。
なお「起動時に CapsLock が OFF なら疑似的に ON にしておく」という先回り対応も検討したが、上記の根本修正だけで不要になったため採用していない。

## GitHub 公開に向けて

- [x] README.md 作成
- [x] LICENSE (MIT) 追加
- [x] .gitignore 追加
- [ ] `git init` / 初回コミット / リモート追加（まだ実施していない）
- [ ] Desktop/StartMenu 以外の実運用ルート（`D:\` 等）を各自の `config.json` に追加する運用ドキュメントで足りているか要確認
